describe("Test type data", () => {
    it("should success", () => {
        let name = "budi";
        let nomor = 10000;
        let isVip = true;

        console.log(name);
        console.log(nomor);
        console.log(isVip);

        // name = 100;
        // nomor = "sera";
    });
});
