describe("Test type data", () => {
  it("should success", () => {
    const name = "budi";
    const nomor = 10000;
    const isVip = true;

    console.log(name);
    console.log(nomor);
    console.log(isVip);

    // name = 100;
    // nomor = "sera";
  });
});
